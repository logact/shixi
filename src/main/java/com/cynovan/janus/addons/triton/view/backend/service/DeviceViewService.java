package com.cynovan.janus.addons.triton.view.backend.service;

import com.cynovan.janus.base.arch.bean.CheckMessage;
import org.springframework.stereotype.Component;

@Component
public class DeviceViewService {

    public CheckMessage clsBindView(String clsCode, String viewCode) {
        CheckMessage checkMessage = CheckMessage.newInstance();
        /*判断当前设备类型是否已绑定view*/

        /**/

        return checkMessage;
    }
}
